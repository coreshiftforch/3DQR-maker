# 開発メモ

`3DQR-maker` の中身のメモ。**なぜそう作ったか**と**踏んだ罠**を残しておくところ。
使い方は [README.md](README.md) を見てください。

## しくみのメモ

- **QR** … `qrcode-generator`（グローバル `qrcode`）。誤り訂正は既定 H。
  黒モジュールを **なるべく大きな長方形にまとめて**（貪欲法）直方体を並べる。
  29モジュールで箱は167個まで減る。
- **止まり穴（磁石の穴）は押し出しだけで作る** … 「穴を開けた板」と「穴のない板」を
  上下に重ねるだけ。閉じたカラどうしが接しているだけなので、スライサーが正しく合体する。
  Three.js に CSG を持ちこまなくて済むし、ブーリアンが壊れる心配もない。
- **1モジュール 0.8mm** が 0.4mmノズルの下限。下回ると警告と「何mm以上にすればよいか」を出す
  （**置かれているQRのうち、いちばん小さいモジュール**で判定する）。Instagram の長いURLは、
  タイル側（48mmの本体で40mm角）だと 0.78mm しかないので 49mm 以上にする。
- 大きさのスライダーには**「既定に戻す」**ボタンがある（キーホルダー48mm／スタンド80mm）。
- **書き出し** … 3MF（無圧縮ZIP＋XMLを自前で組み立て・パーツごとに色）と、
  全パーツを1本にまとめたバイナリSTL。座標はそのまま mm。
- ★**同じ印刷位置のパーツは 3MF でも1つのオブジェクトに束ねる**（`<components>`）。
  1パーツ＝1オブジェクトのまま渡すと、スライサーが**別々の物**として扱うので、
  地続きにしたロゴが本体から離れてしまう。束ねても中身は色ごとの別メッシュのままなので、
  フィラメントは1つずつ割り当てられる。読みこみ側（`parse3MF`）も components をたどる
  ——他ソフトの3MFもこの形で出てくる。
- ★**ダウンロードのファイル名は半角英数だけ**にする（`safeName`）。日本語を入れると
  端末によって**文字化け**し、化けた名前が不正なパスになると
  「データパスが見つかりません」でダウンロードごと失敗する（両方おきた）。
  Windowsで使えない文字と、うしろの「.」「空白」も落とす。
- ★**読みこんだロゴ（アイコン）の大きさは「微調整」で ×1.00〜×1.20**（`state.box.fine`）。
  単位あわせの「倍率」（×1／×1000／×25.4）とは別もので、**`boxSpec()` の一か所で
  `b.scale * b.fine` とかけ算する**だけでよい。ここが `spec` の寸法になり、
  受け口の窓・はめ込む部品のジオメトリ・案内の数字が**全部そろって**大きくなる
  （`boxParts` は spec の `scale` を見ている）。厚み（受け口の深さ）も同じ倍率で伸びる。
  入らなくなった面には、これまでどおり受け口を開けずに「入りませんでした」と出す。
- ★**blob の URL を早く捨ててはいけない**。Chromeが「ダウンロード前に保存場所を確認する」
  設定だと、保存ダイアログを開いているあいだブラウザはまだ blob を読みに来ていない。
  **4秒で `revokeObjectURL` していたので、ダイアログでもたつくと
  「ファイルが見つかりません」で失敗していた**（2026-08-13に修正）。
  いまは `<a>` をいったん本文に付けてから click し、捨てるのは5分後。
  ★この症状は「保存先を確認する」設定の人にしか出ないので、自分の環境では再現しない

## 見た目まわりで踏んだこと

- **右上のQRプレビューは、2枚のとき横に並べる**。縦に積むと背が高くなってビューを覆うし、
  どちらがどちらか分かりにくい。ラベル（QR①/QR②）は 12.5px・太字・濃い色にして、
  canvas にも枠を付けて切れ目をはっきりさせた
- ★**QRプレビューの canvas には `height:auto` を必ず書く**。3Dビュー用の
  `#view canvas{height:100%}` は `#qrpv canvas` と**同じ強さ**で、しかも
  「高さの指定」は `aspect-ratio` より優先される。書き忘れると **QRが縦に引きのばされる**。
  ★ラベルが出る「2枚のとき」しか症状が出ないので、1枚で見ていると気づけない

- **面に出る斜めのシマ＝シャドウアクネ**。`shadow.normalBias`（単位がmmなので 0.6mm）と
  `bias` を入れ、影カメラをモデルの大きさにしぼる（`fitShadow`）と消える
- **曲面の帯＝押し出しっぱなしのフラット法線**。`toCreasedNormals(g, 36°)` で
  ゆるい角だけつなぐと、丸みは滑らかに・直角のかどは立ったままになる
- **スライダーの残像＝値が変わるたびにカメラを置き直していた**こと。OrbitControlsの
  ダンピングと引っぱり合う。`frame(parts, refit)` にして refit のときだけカメラに触る。
  あわせて古いジオメトリを `dispose()`、input は `requestAnimationFrame` で1フレーム1回にまとめる

## 検証済み

- 書き出した 3MF をページ内でZIPとして読み直し、XML・材料・build item・
  三角形インデックスの範囲まで確認
- キーホルダーも 一辺4種 × 磁石3種 × ふち3種 × リング2種の 72 通りで水密・面の向きを確認
  （磁石のポケットは、どの寸法でも必ずわくの内がわに収まる＝壁の下に潜りこまない）
- **ダボ**：磁石4種 × 一辺4種の16通りで水密・面の向き、および
  磁石の穴との最小すきま 5.45mm・タイルのふちまで 4.00mm を確認
- **両面QR**：かたち2種 × うら面3通り（なし／長いURL／日本語）× 一辺4種の24通りで水密・面の向き。
  さらに**うら面のQRを「うら側から見た人の格子」に戻して、元のQRと1マスも違わないこと**
  （＝鏡の向きが正しいこと）と、詰め物の面積が窓とぴったり一致することを確認
- **レイアウト**：キーホルダー6通り・スタンド5通りの割りあて（QR①/QR②/なしの組み合わせ）で
  水密・面の向き。おもて面／うら面それぞれのQRを
  「その面を見る人の格子」に戻して、元のQRと一致することを確認
  （※このときは「ロゴ」も選択肢にあった。2026-08-13に外したぶんは再確認済み）
- **スタンドはスライダーの端まで 243 通り**を自動で回し、全パーツについて
  (1) 有向辺がすべて対で消える＝水密、(2) 符号つき体積が正＝面の向きが外、
  (3) プレートの下前かどと脚の口の外かどが**誤差0でツライチ**、を確認
- QRの静穏帯：URL 3種 × 幅3種 × 余白3種の 27 通りで、
  アーチの輪郭からQRまでの距離が 4モジュール以上あることを確認
- **ロゴの微調整（×1.00〜×1.20）**：かたち2 × 面5 × 四角柱/円柱 × 地続き/別部品 ×
  回転0/90° × 倍率11段の **440通り**で、`spec` の寸法＝`読みこんだ囲み×倍率×微調整`
  （90°ではタテヨコ入れかわり）・窓＝`spec+すきま×2`・はめ込み部品の囲み＝`spec`・
  **奇数の辺0（水密）**・「入った面」と部品の有無が一致、を確認（失敗0）。
  さらに薄い（0.5mm）モデルで**地続きの「板＋ロゴ＝素の板」が15通りとも誤差0**
  （1件だけ 0.0033mm³＝float32の丸め）、詰め物の体積＝`断面積×高さ×倍率³`も一致
- **はめ込み（入れかた2種こみ）**：入れかた2 × かたち2 × 四角柱/円柱 × 大きさ5種 ×
  一辺5種 × 高さ3種 × 面の割りあての **1260通り**で水密・面の向き・ベッド下なし、
  および「どちらのモードでも別パーツ・ロゴの色で出る」を確認（失敗0）。
  ★**地続きは「板＋ロゴ＝素の板」が誤差ゼロ**（すき間も重なりもない）
- **はめ込み**：かたち2種 × 四角柱/円柱 × 大きさ3種 × 板厚3種 × 柱の幅3種 × 高さ3種 ×
  すきま3種 × 面の割りあて（キーホルダー4通り・スタンド3通り）の **3402通り**で
  水密・面の向き・ベッド下なし・**はめこむ部品の体積＝断面積×高さ**を確認（失敗0）。
  さらに「厚くする量が0になる深さ」に固定した12通りで
  **受け口で抜けた体積＝窓の面積×深さ**が誤差0.02mm³以内で一致（四角柱・円柱とも）。
  円の窓は**内接円の半径が狙いどおり**（真円が入る）ことも確認
- **3MFの健全さ**（2026-08-14）：かたち2 × 一辺4種 × 面の割りあて6通り × 寸法/モデル ×
  入れかた2 の **192通り**で、書き出した3MFをZIPとして開いてXMLを読み、
  **オブジェクトごとに頂点番号で辺の枚数を数えて**
  (1) 開いた辺0、(2) 3枚以上の辺0、(3) つぶれた三角形0、(4) 番号が範囲内、
  (5) 符号つき体積が正 → **失敗0**。まとめ刷り（24オブジェクト・51344三角形）でも0
  ※Bambu Studio の CLI は Windows では標準出力を返さないので、スライサーが見ている指標
  （頂点番号での辺のつながり）を自前で測るしかない
- **読みこんだモデルの向き**（2026-08-14）：板＋「上面のかたすみに置いた目印」という
  検査用モデルを作り、かたち2 × 面5 × 入れかた2 の全通りで
  **目印が必ず面の外がわ**に出ることを確認。あわせて
  かたち2 × 面5 × 入れかた2 × 寸法/モデル × 一辺3種の **120通り**で水密・面の向き（失敗0）。
  向きの選択（回転4 × 裏返し2 の8通り）でも、受け口のタテヨコが入れかわること・
  目印の位置・水密を確認
- **★地続き**：入れかた2種 × かたち2種 × 四角柱/円柱 × 大きさ3 × 一辺3 × 高さ3 ×
  面の割りあての **756通り**で、地続きなら別部品が出ないこと・別部品なら数が合うこと、
  および水密・面の向きを確認（失敗0）。
  さらに **板の体積が「素の板」とぴったり同じ**（＝窓と柱にすき間も重なりもない）ことを、
  四角柱・円柱 × 一辺5種の10通りで**誤差ゼロ**で確認
- **モデルの読みこみ**：同じ四角柱（18×18×3mm・原点から離した位置）を
  **STL（バイナリ／ASCII）・OBJ・3MF** で書き出して読み直し、3形式とも
  寸法・体積972mm³・囲み比1.0 が一致。3MFは `unit="centimeter"` を
  正しくmmに直すことも確認。倍率×1000のときは「入りません」と出ること。
  UIのファイル入力からも3形式を通し、22×16×3mm・体積1056mm³ で一致。
  ★**実際に出まわる deflate 圧縮つきの3MF**で試すこと（無圧縮だけでは通らない道がある）
- ★**3MFのマテリアル**：2色（下半分＝赤・上半分＝青、三角形ごとに `pid`/`p1`）の3MFを読み、
  **色2つを検出 → 色ごとに別パーツ（e8342c / 1b63d8）→ 書き出した3MFにも両色が残る**
  ことを確認。読みこみ2種 × 入れかた2種 × かたち2種 × 大きさ3種 × 一辺3種 ×
  割りあての **360通り**で水密・面の向き・パーツ数（色の数ぶん増える）も確認（失敗0）
- ★**QRタイルを下向きにした件**：一辺5種 × 磁石3種 × 彫り3種 × おもて5通り × うら3通りの
  **675通り**で水密・面の向き・「QR面がベッド側」「組み立てで合わせ面がぴったり合う」を確認（失敗0）。
  さらに**組み立てた姿勢のQRを「おもてから見た人の格子」に戻して、元のQRと1マスも違わないこと**を
  URL3種 × 一辺3種の9通りで確認（＝鏡文字になっていない）
- **まとめ刷り**：かたち2種 × プレート5種 × セット数9通り（1〜24）の 90 通りで
  (1) どの部品も**プレートのふち−5mm の内がわ**に収まる、
  (2) **部品どうしのすきまが 4mm を下回らない**（最小 3.999998mm ＝ float32 の丸めぶんだけ）、
  (3) パーツ数がちょうど「1セットぶん × セット数」、を確認。
  さらに 18 通りで**ねかせたあとも水密・面の向きが外**・三角形の数と体積が
  ぴったりセット数倍になることと、3MFをZIPとして読み直して
  オブジェクト／build item／材料が 25 個ずつ・頂点番号が範囲内・座標がプレート内、を確認
- **セット数のスライダーと大きさのスライダーを60回動かしても
  `renderer.info.memory.geometries` は 17 のまま**（まとめ刷りは同じ形を何十個も出すので、
  作り直しのたびに捨てそこねると一気に溜まる）

### ★穴のふちが輪郭の頂点に「ぴったり触れる」と壊れる

`ExtrudeGeometry` は、穴のふちが外の輪郭の頂点にちょうど接すると三角形分割が壊れ、
**面が数枚あまって水密でなくなる**。

実例：**タイル 32mm角・角丸R4 に 24mm の窓**。窓の辺（x=±12）が、角丸の始まる頂点
(12, 16) をちょうど通る。**24.0mm のときだけ**壊れ、23.5でも24.5でも起きない。

→ `slab()` を**自己修復**にした。作ったあとに有向辺を数え、壊れていたら
穴を **0.02%** だけ伸び縮みさせて作り直す（12mmで0.0012mm＝印刷では無いに等しい）。
穴のある板ぜんぶに効くので、同じ種類の当たりを今後も自動でかわせる。

### ★3MFの頂点を溶接していなかった（スライサーが毎回「修復」を要求していた）

Three.js のジオメトリ（`ExtrudeGeometry`・`toCreasedNormals`）は **index を持たない**。
`meshXML` はそれをそのまま書いていたので、**三角形ごとに頂点が3つずつ独立**していた。
STLは頂点番号を持たないので問題にならないが、**3MFは番号でつながりを見る**ので、
スライサーからは「**辺がひとつも共有されていない壊れたモデル**」に見える。

実測：本体パーツは 1376 三角形に対して **開いた辺 4128 ＝ 3×1376（全部）**。
Bambu Studio が毎回「修復」を出し、修復せずに刷ると失敗していた（2026-08-14 ユーザー報告）。

→ `meshXML` で**同じ座標の頂点を同じ番号にまとめる**（1e-4 mm で丸めて突きあわせ）。
これで閉じたカラは辺が必ず2枚ずつになる。ファイルも 2.1MB → 0.83MB に減った。

★**まとめるのは「カラ（閉じた立体）ごと」**。ここをまたいでまとめてはいけない。
このアプリは「閉じたカラを重ねて、スライサーに合体させる」作りなので、
重ねた板どうしは面がぴったり接している。またいでまとめると**辺が4枚**になってしまう。

- ふつうのジオメトリは 1つ＝1カラなので、`geoms` の1つごとに突きあわせ表を作り直す
- ★**例外が `boxesGeom`**（QRのモジュール）。1つのジオメトリに**箱が何十個も**入っていて、
  となり合う箱は面が接している。→ `g.userData.shell = 12`（1箱＝12三角形）を持たせ、
  `meshXML` はその区切りで表をクリアする。これで **3枚以上の辺 623 → 0**

### ★読みこんだモデルを「裏返さずに」受け口へ入れていた

受け口はどの面も、その部品の**ローカル −Z がわ**に開いている（スタンドのおもてだけ +Z）。
いっぽう読みこんだモデルは `normalizeMesh` が `min.z = 0` にそろえるので、
**+Z がわが見せたい面**になっている。そのまま入れていたので、
**底が表に出て、絵も逆さ向き**に見えていた（ユーザーの報告：
「背面が前面に出てきてて、ロゴが左右反転していました」）。

→ `boxParts(b, z0, cy, down)` の `down` で **Y軸まわりに180°まわして**入れる。
180°の回転は鏡ではないので、**文字が反転することはない**。
刷るときも見せたい面がベッドに付くので、そのほうがきれいに出る。

★**自分で作る四角柱・円柱は上下対称なので、この間違いは絶対に表に出ない**。
読みこんだモデルで初めて分かる。総当たりの検査も、対称な柱でやっていたら素通りする——
なので**上面のかたすみに目印を付けた検査用モデル**を作って、
目印が面の外がわに出ることを見るようにした。

なお、どちらの面を見せたいか・どっちが上かは**モデルしだいで決めようがない**ので、
「ロゴの向き」（回転4通り＋裏返し）を人が選べるようにしてある。

### ★地続きなのに、刷る位置を離して並べていた

「地続き（すきま0）」は**本体とくっついた1つの実物**なのに、
はめ込みの `print` 位置を本体からズラして置いていた。
そのままでは刷れないし、3MFでも別オブジェクトになるのでスライサーで別々に出てくる。

→ 地続きのときは**本体と同じ印刷位置**にする。
`printPieces` は印刷位置でまとめるので、まとめ刷りでも一緒に動くようになる。
さらに 3MF 書き出しで、同じ印刷位置のものを `<components>` で**1オブジェクトに束ねる**。

### 断面をいじるときに踏んだ罠

- **同じ線を行って戻る「ヒゲ」を輪郭に作らない**。受け口の内がわのかどを打ってから
  内面をたどると、長さ0の折り返しができて三角形分割が壊れる（面が1枚余って水密でなくなる）。
  出るかどうかはパラメータ次第なので、**気づかないまま出荷されうる**
- **重心を出すときは、面積でなく体積で重み付けする**。プレートは「板の面」、
  脚は「真横から見た断面」で、面積の意味がまるで違う（押し出し長さも T と W で違う）

## これから

- 実機ではめ合いの確認（すきま片側 0.20mm で固すぎ／ゆるすぎがないか）
- はめ込みの 3MF / OBJ 読みこみ（いまは STL だけ）
- GitHub Pages への公開
- QRタイルを**裏返しても はまる**ようにする案：おもても彫りこみにして両面をツライチにし、
  本体側の磁石2個を N上・S上 と互いちがいに入れる（左右を返しても極が合う）
